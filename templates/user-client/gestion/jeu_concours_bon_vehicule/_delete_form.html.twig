<form method="post" action="{{ path('jeu_concours_bon_vehicule_delete', {'id': jeu_concours_bon_vehicule.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jeu_concours_bon_vehicule.id) }}">
    <button class="btn">Delete</button>
</form>
